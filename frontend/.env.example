# ── Local dev (Kali has no backend locally — always points to Render) ──
VITE_API_BASE_URL=https://smartfish-api.onrender.com/api

# ── Production (Vercel) ──────────────────────────────────────────────
# Set this in Vercel's dashboard → Project → Settings → Environment Variables
# VITE_API_BASE_URL=https://smartfish-api.onrender.com/api
